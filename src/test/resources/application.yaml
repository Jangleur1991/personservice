spring:
  application:
    name: person-service

  datasource:
    url: jdbc:mysql://localhost:3306/myDB
    username: jan
    password: password

  jpa:
    hibernate:
      ddl-auto: validate
#    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true